import puppeteer from 'puppeteer'; import path from 'path'; import { fileURLToPath } from 'url'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); (async () => { try { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({ width: 1200, height: 1600, deviceScaleFactor: 2 }); await page.goto('http://localhost:5173/resume-print', { waitUntil: 'networkidle0' }); const pdfPath = path.join(process.cwd(), 'public', 'resume', 'CV.pdf'); await page.pdf({ path: pdfPath, format: 'A4', printBackground: true, scale: 0.8, margin: { top: '0px', right: '0px', bottom: '0px', left: '0px' }, preferCSSPageSize: true }); console.log('Resume PDF generated successfully at:', pdfPath); await browser.close(); } catch (err) { console.error('Error generating PDF:', err); process.exit(1); } })();
